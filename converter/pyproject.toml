[tool.poetry]
name = "converter"
version = "1.0.0"
description = ""
authors = ["dfathree"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
nkf = "^1.0.4"
pyyaml = "^6.0.2"
pytz = "^2024.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
